trigger:
- main

pool:
  name: Default

steps:
- script: |
    npm install @playwright/test
    npx playwright install chromium
    npx playwright test --project=chromium
  displayName: 'Install and run Playwright E2E tests'